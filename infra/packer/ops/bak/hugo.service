[Unit]
Description=Hugo Static Site Server
After=network.target

[Service]
User=ubuntu
WorkingDirectory=/opt/hugo/site
ExecStart=/usr/local/bin/hugo server --bind 0.0.0.0 --port 8080 --baseURL https://onwuachi.com
Restart=always

[Install]
WantedBy=multi-user.target

